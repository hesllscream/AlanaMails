---
/**
 * Hero - Sección principal con estilo dark premium
 * Diseño adaptativo con sistema de espaciado responsivo
 */
import PersonalNote from '@/ui/components/atoms/PersonalNote/PersonalNote.astro'
import DynamicTag from '@/ui/components/atoms/DynamicTag/DynamicTag.astro'
import Button from '@/ui/components/atoms/Button/Button.astro'
import VSLContainer from '@/ui/components/molecules/VSLContainer/VSLContainer.astro'

interface Props {
  name?: string
  platform?: string
}

const { name = '[NOMBRE]', platform = '[PLATAFORMA]' } = Astro.props
---

<section class="section-viewport relative pt-8 md:pt-16 lg:pt-20">
  <div class="max-w-7xl mx-auto px-4 md:px-6 lg:px-12 relative z-10">
    <div class="text-center max-w-4xl mx-auto">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 bg-cr-card border border-cr-purple/30 px-4 py-2 rounded-full mb-6 md:mb-8 animate-fade-in-up transition-all duration-300 hover:border-cr-pink/50 hover:bg-cr-pink/10">
        <span class="w-2 h-2 bg-cr-pink rounded-full animate-pulse"></span>
        <span class="text-xs uppercase tracking-widest text-cr-lavender font-medium">Solo para ti reina, {name}... 👁</span>
      </div>
      
      <h1 class="text-3xl md:text-5xl lg:text-6xl leading-[1.1] mb-4 md:mb-6 font-serif animate-fade-in-up delay-100">
        <span class="text-cr-text">Hola </span>
        <span class="bg-gradient-to-r from-cr-pink via-cr-lavender to-cr-purple bg-clip-text text-transparent">{name}</span>
        <span class="text-cr-text">,</span>
        <br class="hidden sm:block" />
        <span class="text-cr-text">tu perfil de </span>
        <span class="bg-gradient-to-r from-cr-purple via-cr-lavender to-cr-pink bg-clip-text text-transparent">{platform}</span>
        <br class="hidden sm:block" />
        <span class="text-cr-text">tiene una </span>
        <span class="text-cr-pink">fuga de dinero</span>
      </h1>
      
      <p class="text-base md:text-lg lg:text-xl text-cr-muted mb-6 md:mb-8 leading-relaxed max-w-2xl mx-auto font-light animate-fade-in-up delay-200">
        Amiga, analicé tu cuenta hace unas horas y: tienes el tráfico y la estética perfecta, 
        perooooo tu backend está perdiendo el <strong class="text-cr-pink font-semibold">30% de tus ventas totales!!!</strong>
      </p>

      <!-- Stats Row -->
      <div class="flex flex-wrap justify-center gap-6 md:gap-10 mb-8 md:mb-10 animate-fade-in-up delay-300">
        <div class="text-center">
          <p class="text-2xl md:text-3xl font-serif text-cr-lavender">-30%</p>
          <p class="text-xs text-cr-muted uppercase tracking-wider">Ventas perdidas</p>
        </div>
        <div class="text-center">
          <p class="text-2xl md:text-3xl font-serif text-cr-pink">~4h</p>
          <p class="text-xs text-cr-muted uppercase tracking-wider">Respuesta DMs</p>
        </div>
        <div class="text-center">
          <p class="text-2xl md:text-3xl font-serif text-cr-purple">$2,400</p>
          <p class="text-xs text-cr-muted uppercase tracking-wider">Dejados/mes</p>
        </div>
      </div>

      <!-- VSL Container -->
      <div class="mb-8 md:mb-10 animate-fade-in-up delay-400">
        <VSLContainer 
          title={`Auditoría Rápida para ${name}`}
          duration="0:58"
        />
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-up delay-500">
        <a 
          href="#solucion" 
          class="group relative px-8 py-4 bg-gradient-to-r from-cr-pink to-cr-purple text-white rounded-lg text-sm uppercase font-bold tracking-wider text-center overflow-hidden transition-all duration-300 hover:shadow-lg hover:shadow-cr-purple/40 hover:-translate-y-0.5"
        >
          <span class="relative z-10 flex items-center justify-center gap-2">
            ✨ Ver cómo arreglarlo
          </span>
        </a>
        <a 
          href="#pricing" 
          class="group relative px-8 py-4 border-2 border-cr-purple/30 text-cr-text rounded-lg text-sm uppercase font-bold tracking-wider flex items-center justify-center gap-2 overflow-hidden transition-all duration-300 hover:border-cr-pink hover:text-white hover:shadow-lg hover:shadow-cr-pink/30 hover:-translate-y-0.5"
        >
          <span class="absolute inset-0 bg-gradient-to-r from-cr-pink/0 via-cr-purple/0 to-cr-pink/0 group-hover:from-cr-pink group-hover:via-cr-purple group-hover:to-cr-pink transition-all duration-500"></span>
          <span class="relative z-10">Ver oferta exclusiva</span>
          <span class="relative z-10 group-hover:translate-x-1 transition-transform duration-300">→</span>
        </a>
      </div>
      
      <p class="mt-4 text-xs text-cr-muted/70 animate-fade-in-up delay-600">*Este enlace expira en 48 horas.</p>
    </div>
  </div>
</section>
